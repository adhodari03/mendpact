schema_version = "mendpact.policy.v1"
name = "production"
profile = "production"

# These values are the production defaults and are repeated for review clarity.
scan_fail_on = "high"
contract_fail_on = "risky"
allow_private = false
allow_insecure_http = false

# For a protected target, store the token outside the repository and uncomment:
# bearer_token_env = "MENDPACT_ACCESS_TOKEN"
